package com.zerobase.travel.type;

public enum ParticipationStatus {
    JOIN_READY,
    JOIN,
    JOIN_CANCEL,
    JOIN_CANCEL_REFUNDED,
    JOIN_CANCEL_PENALTY,
    TRAVEL_FINISHED,
    TRAVEL_FINISHED_RATED,
    TRAVEL_FINISHED_REFUNDED,

}
